import { SettingsPanel } from "@/features/settings/SettingsPanel";

export function SettingsPage() {
  return (
    <div className="exam-page exam-page--settings">
      <SettingsPanel />
    </div>
  );
}
