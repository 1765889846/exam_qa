# 溯知 Web UI（/sz · /sz-cfg）Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 用手写 HTML/CSS/JS 交付双挂载工作台 `/sz` 与设置页 `/sz-cfg`，同端口调用现有 `/api/v1`（含问答 SSE），无前端构建链。

**Architecture:** FastAPI 分别 `mount` `www/sz` → `/sz`、`www/sz-cfg` → `/sz-cfg`；共享逻辑在 `www/shared/`（不挂载）。浏览器用 `fetch` + SSE（`ReadableStream`/`fetch` 读 `text/event-stream`）调 API——**纯静态不影响问答交互**，因页面与 API 同源，无 CORS 障碍，流式与非流式均为标准 Web API。

**Tech Stack:** 原生 HTML/CSS/JS（ES modules）、CSS 变量双主题、KaTeX 静态资源、FastAPI `StaticFiles`、现有 pytest。

**Spec:** [docs/05-WebUI规划.md](../../05-WebUI规划.md)

---

## 纯静态 vs 问答（说明）

| 能力 | 是否可行 | 做法 |
|---|---|---|
| 上传/列表/删除/扫描 | 是 | `FormData` + `fetch` |
| 流式问答 | 是 | `POST /api/v1/ask` `stream:true`，读 SSE `data:` 行 |
| KaTeX | 是 | 静态 `katex.min.js` + `renderMathInElement` |
| 配置读写 | 是 | `GET/PATCH /api/v1/config` |
| 主题 | 是 | `localStorage` + `matchMedia` |

不需要 WebSocket、不需要 SSR、不需要打包器。

---

## File map

| Path | Role |
|---|---|
| `src/main.py` | 双 mount；`/` → `/sz/`；检测 `www/sz` |
| `src/utils/banner.py` | （若需要）UI 文案已由 main 传入 URL |
| `tests/test_api.py` | 根重定向：无 UI → `/docs`；有 `www/sz` → `/sz/` |
| `www/shared/css/tokens.css` | `--sz-*` 亮/暗 token |
| `www/shared/js/api.js` | `apiGet/Post/Patch/Delete` + unwrap |
| `www/shared/js/theme.js` | system/light/dark |
| `www/shared/js/shell.js` | 顶栏 DOM、health、课程、页间链接 |
| `www/shared/lib/katex/*` | KaTeX 静态文件 |
| `www/sz/index.html` | 工作台壳 |
| `www/sz/css/workbench.css` | 双栏 + 独立滚动 |
| `www/sz/js/workbench.js` | documents + ask SSE + citations |
| `www/sz-cfg/index.html` | 设置壳 |
| `www/sz-cfg/css/settings.css` | 左列表右表单 |
| `www/sz-cfg/js/settings.js` | 全量 config 表单 |

---

### Task 1: 后端双挂载 + 根重定向测试

**Files:**
- Modify: `src/main.py`
- Modify: `tests/test_api.py`

- [ ] **Step 1: 扩展根重定向测试**

在 `tests/test_api.py` 的 `TestRootRedirect` 中追加（保留原「无 www → /docs」）：

```python
import shutil
from pathlib import Path

class TestRootRedirect:
    def test_root_goes_to_docs_without_www(self):
        r = client.get("/", follow_redirects=False)
        assert r.status_code in (307, 302)
        assert r.headers["location"].endswith("/docs")

    def test_root_goes_to_sz_when_sz_exists(self, tmp_path, monkeypatch):
        # 仅当实现改为检测 www/sz 后启用；用 monkeypatch 指向临时目录
        from src import main as main_mod

        www = tmp_path / "www"
        (www / "sz").mkdir(parents=True)
        (www / "sz" / "index.html").write_text("<!doctype html><title>sz</title>", encoding="utf-8")
        (www / "sz-cfg").mkdir(parents=True)
        (www / "sz-cfg" / "index.html").write_text("<!doctype html><title>cfg</title>", encoding="utf-8")
        monkeypatch.setattr(main_mod, "_WWW_DIR", www)
        # 注意：StaticFiles 在 import 时已挂载；本测试若难以热替换 mount，
        # 则改为只断言 root() 逻辑可抽成函数后单测。见 Step 3。
```

若 `StaticFiles` 在模块 import 时已绑定导致难以 monkeypatch，改为：

```python
# src/main.py 抽出：
def _ui_ready(www_dir: Path) -> bool:
    return (www_dir / "sz" / "index.html").is_file()

def _root_redirect_url(www_dir: Path) -> str:
    return "/sz/" if _ui_ready(www_dir) else "/docs"
```

测试只测这两个纯函数：

```python
def test_root_redirect_helpers(tmp_path):
    from src.main import _ui_ready, _root_redirect_url
    assert _root_redirect_url(tmp_path) == "/docs"
    (tmp_path / "sz").mkdir()
    (tmp_path / "sz" / "index.html").write_text("x", encoding="utf-8")
    assert _ui_ready(tmp_path) is True
    assert _root_redirect_url(tmp_path) == "/sz/"
```

- [ ] **Step 2: 跑测试，确认新用例先失败或 helpers 未定义**

Run: `uv run pytest tests/test_api.py::TestRootRedirect -q`  
（若用 helpers：`uv run pytest tests/test_api.py -k root_redirect -q`）  
Expected: FAIL（`_ui_ready` 未定义或仍指向 `/index.html`）

- [ ] **Step 3: 改 `src/main.py` 挂载与根路由**

把文末静态挂载与 `root` 换成（保留既有 import/`api_router`）：

```python
from pathlib import Path
from fastapi.staticfiles import StaticFiles
from fastapi.responses import RedirectResponse

_WWW_DIR = Path(__file__).resolve().parents[1] / "www"
_SZ_DIR = _WWW_DIR / "sz"
_SZ_CFG_DIR = _WWW_DIR / "sz-cfg"


def _ui_ready(www_dir: Path | None = None) -> bool:
    root = www_dir if www_dir is not None else _WWW_DIR
    return (root / "sz" / "index.html").is_file()


def _root_redirect_url(www_dir: Path | None = None) -> str:
    return "/sz/" if _ui_ready(www_dir) else "/docs"


@app.get("/")
async def root():
    return RedirectResponse(url=_root_redirect_url())


# 启动摘要里 ui_mounted / web_ui 改为：
# ui_mounted = _ui_ready()
# web_ui = f"http://{config.host}:{config.port}/sz/"

# 必须在 include_router 之后、且不要 mount("/")：
if _SZ_DIR.is_dir():
    app.mount("/sz", StaticFiles(directory=str(_SZ_DIR), html=True), name="sz")
if _SZ_CFG_DIR.is_dir():
    app.mount(
        "/sz-cfg",
        StaticFiles(directory=str(_SZ_CFG_DIR), html=True),
        name="sz_cfg",
    )
```

同步修改 `main.py` 里原来 `ui_mounted = _WWW_DIR.exists()` 与 `web_ui=.../` 的调用点（约 lifespan/banner 处）使用 `_ui_ready()` 与 `/sz/`。

- [ ] **Step 4: 再跑测试**

Run: `uv run pytest tests/test_api.py -q`  
Expected: PASS（含 redirect helpers / 原有 API 用例）

- [ ] **Step 5: Commit**（若用户要求提交时再执行）

```bash
git add src/main.py tests/test_api.py
git commit -m "feat(web): mount /sz and /sz-cfg, redirect / to /sz"
```

---

### Task 2: shared tokens + theme + api

**Files:**
- Create: `www/shared/css/tokens.css`
- Create: `www/shared/js/theme.js`
- Create: `www/shared/js/api.js`

- [ ] **Step 1: 写 `tokens.css`**

```css
:root {
  --sz-font: "IBM Plex Sans", "Noto Sans SC", system-ui, sans-serif;
  --sz-radius-sm: 6px;
  --sz-radius-md: 8px;
  --sz-radius-lg: 12px;
  --sz-accent: #2a9d8f;
  --sz-success: #2da44e;
  --sz-danger: #cf222e;
  --sz-bg-base: #f0f2f4;
  --sz-bg-panel: #ffffff;
  --sz-bg-surface: #f5f7f8;
  --sz-text: #0c1118;
  --sz-text-muted: #6b7280;
  --sz-border: #e2e6eb;
}

:root[data-theme="dark"] {
  --sz-accent: #4ecdc4;
  --sz-success: #3fb950;
  --sz-danger: #f85149;
  --sz-bg-base: #0c1118;
  --sz-bg-panel: #151b24;
  --sz-bg-surface: #121921;
  --sz-text: #e8eaed;
  --sz-text-muted: #8b939e;
  --sz-border: #252b35;
}

@media (prefers-reduced-motion: reduce) {
  *, *::before, *::after {
    animation-duration: 0.01ms !important;
    transition-duration: 0.01ms !important;
  }
}
```

- [ ] **Step 2: 写 `theme.js`**

```js
const KEY = "sz.theme"; // system | light | dark

export function getThemePref() {
  return localStorage.getItem(KEY) || "system";
}

export function resolveTheme(pref = getThemePref()) {
  if (pref === "light" || pref === "dark") return pref;
  return window.matchMedia("(prefers-color-scheme: dark)").matches
    ? "dark"
    : "light";
}

export function applyTheme(pref = getThemePref()) {
  document.documentElement.dataset.theme = resolveTheme(pref);
}

export function setThemePref(pref) {
  localStorage.setItem(KEY, pref);
  applyTheme(pref);
}

export function initTheme() {
  applyTheme();
  const mq = window.matchMedia("(prefers-color-scheme: dark)");
  mq.addEventListener("change", () => {
    if (getThemePref() === "system") applyTheme("system");
  });
}
```

- [ ] **Step 3: 写 `api.js`**

```js
const BASE = "/api/v1";

async function parse(res) {
  const body = await res.json().catch(() => ({}));
  if (!res.ok || (body.code != null && body.code >= 400)) {
    const msg = body.message || res.statusText || "请求失败";
    throw new Error(msg);
  }
  return body.data !== undefined ? body.data : body;
}

export async function apiGet(path, params) {
  const url = new URL(BASE + path, window.location.origin);
  if (params) {
    Object.entries(params).forEach(([k, v]) => {
      if (v != null && v !== "") url.searchParams.set(k, v);
    });
  }
  return parse(await fetch(url));
}

export async function apiPost(path, json) {
  return parse(
    await fetch(BASE + path, {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify(json),
    }),
  );
}

export async function apiPatch(path, json) {
  return parse(
    await fetch(BASE + path, {
      method: "PATCH",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify(json),
    }),
  );
}

export async function apiDelete(path) {
  return parse(await fetch(BASE + path, { method: "DELETE" }));
}

export async function apiUpload(path, formData) {
  return parse(await fetch(BASE + path, { method: "POST", body: formData }));
}

/** 消费 ask SSE：onEvent({type, ...}) */
export async function apiAskStream(body, onEvent, signal) {
  const res = await fetch(BASE + "/ask", {
    method: "POST",
    headers: { "Content-Type": "application/json" },
    body: JSON.stringify({ ...body, stream: true }),
    signal,
  });
  if (!res.ok) {
    const err = await res.json().catch(() => ({}));
    throw new Error(err.message || res.statusText);
  }
  const reader = res.body.getReader();
  const dec = new TextDecoder();
  let buf = "";
  while (true) {
    const { done, value } = await reader.read();
    if (done) break;
    buf += dec.decode(value, { stream: true });
    const parts = buf.split("\n\n");
    buf = parts.pop() || "";
    for (const chunk of parts) {
      for (const line of chunk.split("\n")) {
        if (!line.startsWith("data:")) continue;
        const raw = line.slice(5).trim();
        if (!raw) continue;
        onEvent(JSON.parse(raw));
      }
    }
  }
}
```

- [ ] **Step 4: 自检** — 文件存在且无语法错误即可（浏览器侧 Task 3 再挂）。

- [ ] **Step 5: Commit**（若用户要求）

```bash
git add www/shared
git commit -m "feat(web): add shared tokens, theme, and api client"
```

---

### Task 3: shell 顶栏 + 两页空壳

**Files:**
- Create: `www/shared/js/shell.js`
- Create: `www/shared/css/shell.css`
- Create: `www/sz/index.html`
- Create: `www/sz/css/workbench.css`（先最小布局）
- Create: `www/sz/js/workbench.js`（先 `initShell`）
- Create: `www/sz-cfg/index.html`
- Create: `www/sz-cfg/css/settings.css`
- Create: `www/sz-cfg/js/settings.js`

- [ ] **Step 1: `shell.css` + `shell.js`**

`shell.css`：顶栏 flex、nav 链接、健康点、小屏隐藏次要链到 `.sz-nav-more`。

`shell.js` 核心：

```js
import { apiGet } from "./api.js";
import { getThemePref, setThemePref, initTheme } from "./theme.js";

const COURSE_KEY = "sz.course_id";

export function getCourseId() {
  return localStorage.getItem(COURSE_KEY) || "";
}

export function setCourseId(id) {
  localStorage.setItem(COURSE_KEY, id);
}

export async function initShell({ active }) {
  initTheme();
  const root = document.querySelector("[data-sz-shell]");
  if (!root) return;

  root.innerHTML = `
    <header class="sz-topbar">
      <a class="sz-brand" href="/sz/">溯知</a>
      <nav class="sz-nav">
        <a href="/sz/" data-nav="sz" class="${active === "sz" ? "is-active" : ""}">工作台</a>
        <a href="/sz-cfg/" data-nav="cfg" class="${active === "cfg" ? "is-active" : ""}">设置</a>
      </nav>
      <div class="sz-topbar-right">
        <select id="sz-college" aria-label="学院"></select>
        <select id="sz-course" aria-label="课程"></select>
        <span id="sz-health" class="sz-health" title="health">●</span>
        <button type="button" id="sz-theme" aria-label="主题">主题</button>
      </div>
    </header>
    <div id="sz-toast" class="sz-toast" hidden></div>
  `;

  document.getElementById("sz-theme").onclick = () => {
    const order = ["system", "light", "dark"];
    const i = order.indexOf(getThemePref());
    setThemePref(order[(i + 1) % order.length]);
  };

  await Promise.all([refreshHealth(), loadCatalog()]);
  setInterval(refreshHealth, 15000);
}

async function refreshHealth() {
  const el = document.getElementById("sz-health");
  if (!el) return;
  try {
    const data = await apiGet("/health");
    const ok = data?.status === "ok" || data?.status === "healthy";
    el.dataset.state = ok ? "ok" : "degraded";
    el.title = JSON.stringify(data);
  } catch {
    el.dataset.state = "down";
  }
}

async function loadCatalog() {
  const collegeSel = document.getElementById("sz-college");
  const courseSel = document.getElementById("sz-course");
  if (!collegeSel || !courseSel) return;
  const colleges = await apiGet("/colleges");
  collegeSel.innerHTML = (colleges || [])
    .map((c) => `<option value="${c.id}">${c.name || c.id}</option>`)
    .join("");
  async function reloadCourses() {
    const list = await apiGet("/courses", { college_id: collegeSel.value });
    courseSel.innerHTML = (list || [])
      .map((c) => `<option value="${c.id}">${c.name || c.id}</option>`)
      .join("");
    const saved = getCourseId();
    if (saved && [...courseSel.options].some((o) => o.value === saved)) {
      courseSel.value = saved;
    } else if (courseSel.value) {
      setCourseId(courseSel.value);
    }
  }
  collegeSel.onchange = reloadCourses;
  courseSel.onchange = () => setCourseId(courseSel.value);
  if (colleges?.length) await reloadCourses();
}

export function toast(msg, kind = "info") {
  const el = document.getElementById("sz-toast");
  if (!el) return;
  el.hidden = false;
  el.dataset.kind = kind;
  el.textContent = msg;
  clearTimeout(el._t);
  el._t = setTimeout(() => {
    el.hidden = true;
  }, 3200);
}
```

> 若 `/health` 返回字段名不是 `status`，以实际 `GET /api/v1/health` 响应为准改 `refreshHealth`（先 `uv run` 看一眼或读 `health.py`）。

- [ ] **Step 2: 两页 HTML 骨架**

`www/sz/index.html`：

```html
<!doctype html>
<html lang="zh-CN">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>溯知 · 工作台</title>
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link href="https://fonts.googleapis.com/css2?family=IBM+Plex+Sans:wght@400;500;600;700&family=Noto+Sans+SC:wght@400;500;600;700&display=swap" rel="stylesheet" />
    <link rel="stylesheet" href="../shared/css/tokens.css" />
    <link rel="stylesheet" href="../shared/css/shell.css" />
    <link rel="stylesheet" href="./css/workbench.css" />
  </head>
  <body>
    <div data-sz-shell></div>
    <main class="sz-workbench">
      <section class="sz-panel sz-docs" id="sz-docs-panel">
        <div class="sz-docs-upload" id="sz-upload-zone">上传区</div>
        <div class="sz-docs-list" id="sz-docs-list"></div>
      </section>
      <section class="sz-panel sz-ask" id="sz-ask-panel">
        <div class="sz-ask-stream" id="sz-ask-stream"></div>
        <form class="sz-ask-composer" id="sz-ask-form">
          <textarea id="sz-ask-input" rows="2" placeholder="输入问题…"></textarea>
          <button type="submit">提问</button>
        </form>
      </section>
    </main>
    <script type="module" src="./js/workbench.js"></script>
  </body>
</html>
```

`www/sz-cfg/index.html`：同样引入 tokens/shell；`main` 为 `.sz-config` 左 `#sz-config-nav` 右 `#sz-config-main`；`active` 用 cfg。

- [ ] **Step 3: workbench/settings 入口只 initShell**

```js
// www/sz/js/workbench.js
import { initShell } from "../../shared/js/shell.js";
initShell({ active: "sz" });
```

```js
// www/sz-cfg/js/settings.js
import { initShell } from "../../shared/js/shell.js";
initShell({ active: "cfg" });
```

- [ ] **Step 4: 手工验证**

```bash
uv run exam
# 打开 http://127.0.0.1:<PORT>/sz/
# 打开 http://127.0.0.1:<PORT>/sz-cfg/
# / → 应到 /sz/
```

Expected: 顶栏可见、主题可切、两页互链。

- [ ] **Step 5: Commit**（若用户要求）

---

### Task 4: 工作台 · 资料库

**Files:**
- Modify: `www/sz/js/workbench.js`
- Modify: `www/sz/css/workbench.css`

- [ ] **Step 1: 列表 / 上传 / 删除 / 扫描**

在 `workbench.js` 增加（需已选 `course_id`）：

```js
import {
  apiGet, apiDelete, apiUpload, apiPost,
} from "../../shared/js/api.js";
import { getCourseId, initShell, toast } from "../../shared/js/shell.js";

async function refreshDocs() {
  const courseId = getCourseId();
  const box = document.getElementById("sz-docs-list");
  if (!courseId) {
    box.textContent = "请先选择课程";
    return;
  }
  const data = await apiGet("/documents", { course_id: courseId });
  const items = data?.items || data || [];
  box.innerHTML = items
    .map(
      (d) => `<div class="sz-doc-row" data-id="${d.id}">
        <span>${d.filename || d.id}</span>
        <button type="button" data-del="${d.id}">删除</button>
      </div>`,
    )
    .join("") || "<p class='sz-muted'>暂无资料</p>";
}

// 绑定：upload input change → FormData + course_id → apiUpload("/documents", fd)
// 扫描按钮 → apiPost 用 form 或 query：对照 documents.py（scan 用 Form course_id）
// 删除 → apiDelete(`/documents/${id}?course_id=` 或 body——以 OpenAPI 为准）
```

先读 `src/apis/v1/documents.py` 确认 list/upload/delete/scan 的参数形态，再写死到代码（禁止臆造 query 名）。

- [ ] **Step 2: CSS 独立滚动**

```css
.sz-workbench {
  display: grid;
  grid-template-columns: minmax(240px, 360px) 1fr;
  gap: 0;
  height: calc(100dvh - var(--sz-topbar-h, 56px));
  overflow: hidden;
}
.sz-docs {
  display: flex;
  flex-direction: column;
  min-height: 0;
  border-right: 1px solid var(--sz-border);
  background: var(--sz-bg-surface);
}
.sz-docs-upload { flex-shrink: 0; }
.sz-docs-list { flex: 1; overflow-y: auto; min-height: 0; }
@media (max-width: 768px) {
  .sz-workbench {
    grid-template-columns: 1fr;
    height: auto;
    overflow: visible;
  }
  .sz-docs-list { max-height: 40vh; }
}
```

- [ ] **Step 3: 浏览器验证上传一条 MD → 列表出现 → 删除**

- [ ] **Step 4: Commit**（若用户要求）

---

### Task 5: 工作台 · 问答 SSE + citations + KaTeX

**Files:**
- Create: `www/shared/lib/katex/`（下载官方 min 资源）
- Modify: `www/sz/index.html`（链 katex css/js）
- Modify: `www/sz/js/workbench.js`
- Modify: `www/sz/css/workbench.css`

- [ ] **Step 1: 拉取 KaTeX 静态文件**

```bash
# 在仓库根；版本可固定 0.16.11
mkdir -p www/shared/lib/katex
# 用 curl/Invoke-WebRequest 下载 katex.min.css、katex.min.js、auto-render.min.js、fonts/
```

`index.html` 增加：

```html
<link rel="stylesheet" href="../shared/lib/katex/katex.min.css" />
<script defer src="../shared/lib/katex/katex.min.js"></script>
<script defer src="../shared/lib/katex/auto-render.min.js"></script>
```

- [ ] **Step 2: ask 表单 + SSE**

```js
import { apiAskStream } from "../../shared/js/api.js";

const streamEl = document.getElementById("sz-ask-stream");
const form = document.getElementById("sz-ask-form");
const input = document.getElementById("sz-ask-input");

form.addEventListener("submit", async (e) => {
  e.preventDefault();
  const question = input.value.trim();
  const courseId = getCourseId();
  if (!question || !courseId) {
    toast("需要课程与问题", "error");
    return;
  }
  let answer = "";
  streamEl.innerHTML = `<div class="sz-turn"><div class="sz-q"></div><div class="sz-a" id="sz-live-a"></div><div id="sz-cites"></div></div>`;
  streamEl.querySelector(".sz-q").textContent = question;
  const live = document.getElementById("sz-live-a");
  try {
    await apiAskStream({ question, course_id: courseId, mode: "qa" }, (ev) => {
      if (ev.type === "phase") {
        live.dataset.phase = ev.phase;
      } else if (ev.type === "delta") {
        answer += ev.text || "";
        live.textContent = answer;
      } else if (ev.type === "done") {
        const d = ev.data || {};
        live.textContent = d.answer || answer;
        live.dataset.grounded = String(!!d.grounded);
        renderCitations(document.getElementById("sz-cites"), d.citations || []);
        if (window.renderMathInElement) {
          renderMathInElement(live, {
            delimiters: [
              { left: "$$", right: "$$", display: true },
              { left: "$", right: "$", display: false },
            ],
          });
        }
      } else if (ev.type === "error") {
        toast(ev.message || "问答失败", "error");
      }
    });
  } catch (err) {
    toast(err.message, "error");
  }
});

function renderCitations(el, cites) {
  el.innerHTML = cites
    .map(
      (c, i) => `<details class="sz-cite">
        <summary>[${i + 1}] ${c.source_file} · ${c.score?.toFixed?.(3) ?? ""}</summary>
        <pre>${c.snippet || ""}</pre>
      </details>`,
    )
    .join("");
}
```

回答区 CSS：`.sz-ask { display:flex; flex-direction:column; min-height:0; }` · `.sz-ask-stream { flex:1; overflow-y:auto; }` · `.sz-ask-composer { flex-shrink:0; }`

- [ ] **Step 3: 手工验证** 有资料时提问见流式；无命中见「资料库中未找到相关内容」+ `grounded=false`

- [ ] **Step 4: Commit**（若用户要求）

---

### Task 6: 设置页全量可写

**Files:**
- Modify: `www/sz-cfg/js/settings.js`
- Modify: `www/sz-cfg/css/settings.css`

- [ ] **Step 1: 分组元数据 + 渲染**

```js
const GROUPS = [
  {
    id: "llm",
    title: "LLM",
    fields: [
      { key: "model", label: "模型" },
      { key: "base_url", label: "Base URL" },
      { key: "api_key", label: "API Key", secret: true },
      { key: "timeout", label: "超时", type: "number" },
    ],
  },
  {
    id: "embedding",
    title: "Embedding",
    fields: [
      { key: "provider", label: "Provider" },
      { key: "model", label: "模型" },
      { key: "base_url", label: "Base URL" },
      { key: "api_key", label: "API Key", secret: true },
      { key: "timeout", label: "超时", type: "number" },
    ],
  },
  {
    id: "retrieval",
    title: "检索",
    fields: [
      { key: "top_k", label: "top_k", type: "number" },
      { key: "score_threshold", label: "阈值", type: "number", step: "0.01" },
    ],
  },
  {
    id: "chunk",
    title: "分块",
    fields: [
      { key: "chunk_size", label: "chunk_size", type: "number" },
      { key: "chunk_overlap", label: "overlap", type: "number" },
    ],
  },
  {
    id: "parsing",
    title: "解析 / OCR",
    fields: [
      { key: "pdf_use_ocr", label: "启用 OCR", type: "checkbox" },
      { key: "pdf_force_ocr", label: "强制 OCR", type: "checkbox" },
      { key: "pdf_ocr_language", label: "OCR 语言" },
    ],
  },
  {
    id: "proxy",
    title: "代理",
    fields: [
      { key: "url", label: "URL" },
      { key: "no_proxy", label: "NO_PROXY" },
      { key: "enabled", label: "启用", type: "checkbox" },
    ],
  },
  {
    id: "app",
    title: "上传",
    fields: [{ key: "max_upload_mb", label: "最大上传 MB", type: "number" }],
  },
  {
    id: "server",
    title: "服务",
    fields: [
      { key: "host", label: "Host" },
      { key: "port", label: "Port", type: "number" },
    ],
  },
];
```

加载：`const cfg = await apiGet("/config")`；左侧渲染 `GROUPS`；点击后把 `cfg[group.id]` 填表。保存：`apiPatch("/config", { [groupId]: payload })`；`api_key` 若仍是 `***` 则不要提交该字段（与后端 `MASKED_SECRET` 一致）。展示只读：`cfg.storage`、`cfg.meta`、`settings_effects`。

- [ ] **Step 2: 设置页双栏 CSS**（左约 280px，右 `1fr`；各自 `overflow-y: auto`；`≤768px` 堆叠）

- [ ] **Step 3: 改一个无害字段（如 `retrieval.top_k`）保存 → 刷新仍在；密钥输入框不回显明文**

- [ ] **Step 4: Commit**（若用户要求）

---

### Task 7: 打磨 + 回归清单

**Files:** 按需改 CSS/空状态文案；可选同步 `docs/03-工程规范.md` §3.10 启动说明为 `/sz/`（一行，避免文档撒谎）。

- [ ] **Step 1: 按规格验证清单勾选**

```
- [ ] GET / → /sz/
- [ ] /sz/ ↔ /sz-cfg/ 顶栏互链；主题 system/light/dark
- [ ] 选课 → 上传 → 列表 → 扫描
- [ ] 问答 SSE + citations + KaTeX
- [ ] grounded: false 拒答
- [ ] /sz-cfg/ 各组 PATCH；env 只读提示
- [ ] ≤768px 分区可滚、无横向撑破
```

- [ ] **Step 2: `uv run pytest -q` 确认后端未回归**

- [ ] **Step 3: Commit**（若用户要求）

---

## Spec coverage（自检）

| 规格项 | Task |
|---|---|
| 手写、无构建 | 2–6 |
| `/sz` + `/sz-cfg` 双挂载 | 1 |
| `www/shared` | 2–3 |
| 跟随系统主题 | 2–3 |
| 工作台双栏独立滚动 | 4–5 |
| ask SSE / citations / KaTeX | 5 |
| 设置全量可写 | 6 |
| 小屏 | 4、6、7 |
| `sz-*` 命名 | 全文 |

---

## 执行方式

Plan complete and saved to `docs/superpowers/plans/2026-07-17-sz-webui.md`.

**1. Subagent-Driven（推荐）** — 每任务新开子代理，任务间复查  

**2. Inline Execution** — 本会话按任务连续做，设检查点  

选哪种？
